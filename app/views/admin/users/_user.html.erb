<li>
  <%= gravatar_for user, size: Settings.avatar_small %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %> |
    <%= link_to t("delete"), admin_user_path(user), method: :delete, data: {confirm: t("you_sure")} %>
  <% end %>
</li>
