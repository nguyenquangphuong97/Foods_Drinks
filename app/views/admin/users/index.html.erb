<% provide :title, t(".title") %>
<div class="container">
  <div class="row">
    <%= render "shared/nav_bar" %>
    <div class="col-md-9 well">
      <h2><%= t ".title" %></h2>
      <%= will_paginate %>
      <ul class="users">
        <%= render @users %>
      </ul>
      <%= will_paginate %>
    </div>
  </div>
</div>
